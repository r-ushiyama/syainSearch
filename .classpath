
	    response.setContentType("text/html; charset=shift-jis");
	    if (userId.equals(/*request.getParameter("username")*/Id) == true
	      && password.equals(/*request.getParameter("password")*/Pass) == true) {
	      //セッション情報の新規作成
	      HttpSession session = request.getSession(true);
	      //セッションオブジェクトに保存する
	      session.setAttribute("username", userId);

	      System.out.println("ログインしました");

	      Object ID = session.getAttribute("username");
	      System.out.println(ID);
	   // アクセスした人に応答するためのJSONを用意する
	      PrintWriter pw = response.getWriter();
	      Map <String, String> responseData = new HashMap<>();
	      responseData.put("result", "ok");
	   	// JSONで出力する
	      pw.append(new ObjectMapper().writeValueAsString(responseData));
	    } else {
	      System.out.println("NG パスワードまたはユーザー名が違います");
	    }
	  }